import 'package:flutter/material.dart';

class FavoritesAuthorsScreen extends StatelessWidget {
  const FavoritesAuthorsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("FavoritesAuthorsScreen"),
    );
  }
}
